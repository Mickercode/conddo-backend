target/
!.mvn/wrapper/maven-wrapper.jar
*.class
*.log

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# Local env (never commit secrets)
.env
infra/.env

# JWT signing keys — generated locally / never commit (prod uses env vars).
# Match *.pem anywhere so a key generated at the repo root can't be committed.
*.pem

# OS
.DS_Store
